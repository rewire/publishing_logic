class <%= migration_name %> < ActiveRecord::Migration
  def up
    change_table :<%= table_name %> do |t|
      t.boolean :publishing_enabled
      t.datetime :published_at<% if options[:published_until_field] %>, :published_until<% end %>
      t.index [:published_at, :publishing_enabled<% if options[:published_until_field] %>, :published_until<% end %>], :name => 'index_<%= table_name %>_on_publishing_logic_fields'
    end
  end

  def down
    change_table :<%= table_name %> do |t|
      t.remove_index :name => 'index_<%= table_name %>_on_publishing_logic_fields'
      t.remove :publishing_enabled, :published_at<% if options[:published_until_field] %>, :published_until<% end %>
    end
  end
end
